FROM node:18-alpine
WORKDIR /app

COPY package*.json ./
COPY dist/ ./dist
COPY node_modules/ ./node_modules

RUN mkdir -p ./tmp/images
COPY api/images ./tmp/images

CMD ["node", "dist/api/server.js"]
