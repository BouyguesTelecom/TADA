meta {
  name: DELETE FILES
  type: http
  seq: 3
}

delete {
  url: {{URL_API}}{{URL_API_PREFIX}}/files
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  [{
    "namespace": "DEV",
    "uuids": "{{MULTI_IMAGE_IDS}}"
  }]
}
