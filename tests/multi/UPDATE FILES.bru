meta {
  name: UPDATE FILES
  type: http
  seq: 2
}

patch {
  url: {{URL_API}}{{URL_API_PREFIX}}/files
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  [{
    "namespace": "DEV",
    "uuids": "{{MULTI_IMAGE_IDS}}",
    "information": "Updated multiple files via JSON"
  }]
}
