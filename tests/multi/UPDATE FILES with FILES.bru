meta {
  name: UPDATE FILES with FILES
  type: http
  seq: 4
}

patch {
  url: {{URL_API}}{{URL_API_PREFIX}}/files
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:multipart-form {
  namespace: DEV
  uuids: {{MULTI_IMAGE_IDS}}
  information: Updated files with new files
  files: @file(/tmp/images/error.webp|/tmp/images/test.png)
}
