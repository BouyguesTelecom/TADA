meta {
  name: PATCH FILE
  type: http
  seq: 4
}

patch {
  url: {{URL_API}}{{URL_API_PREFIX}}/file/{{IMAGE_ID}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  {
    "namespace": "DEV",
    "information": "PATCH FILE WITH JSON BODY",
    "expired": false
  }
}
