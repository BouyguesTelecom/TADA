meta {
  name: DELETE CATALOG
  type: http
  seq: 9
}

delete {
  url: {{URL_API}}{{URL_API_PREFIX}}/catalog
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  {
    "namespace": "DEV"
  }
}
