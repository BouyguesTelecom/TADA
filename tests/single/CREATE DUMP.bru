meta {
  name: CREATE DUMP
  type: http
  seq: 1
}

post {
  url: {{URL_API}}{{URL_API_PREFIX}}/catalog/create-dump
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  {
    "namespace": "DEV"
  }
}
