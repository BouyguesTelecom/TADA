meta {
  name: 5 - CLEANUP SETUP IMAGE
  type: http
  seq: 5
}

delete {
  url: {{URL_API}}{{URL_API_PREFIX}}/file/{{SETUP_IMAGE_ID}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  {
    "namespace": "DEV"
  }
}

assert {
  res.status: eq 200
}