meta {
  name: 2 - TEST CATALOG CREATE DUMP
  type: http
  seq: 2
}

post {
  url: {{URL_API}}{{URL_API_PREFIX}}/catalog/create-dump
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  {
    "namespace": "DEV"
  }
}

assert {
  res.status: eq 200
}