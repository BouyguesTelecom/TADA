meta {
  name: 4 - TEST CATALOG MULTIPLE PATCH
  type: http
  seq: 4
}

patch {
  url: {{URL_API}}{{URL_API_PREFIX}}/catalog
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  [{
    "namespace": "DEV",
    "uuid": "{{SETUP_IMAGE_ID}}",
    "information": "Updated via multiple patch test"
  }]
}

assert {
  res.status: eq 200
}